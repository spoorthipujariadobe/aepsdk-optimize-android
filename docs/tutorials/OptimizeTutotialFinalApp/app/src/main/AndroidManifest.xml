<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adobe.marketing.optimizetutorial">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name="com.adobe.marketing.optimizetutorial.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Optimize">
        <activity
            android:name="com.adobe.marketing.optimizetutorial.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/app_name"
            android:theme="@style/Theme.Optimize.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Optimize Tutorial: CODE SECTION 4/10 BEGINS -->
            <intent-filter android:label="assurance_deeplink">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="optimizetutorial"/>
            </intent-filter>
            <!-- Optimize Tutorial: CODE SECTION 4 ENDS -->
        </activity>
    </application>

</manifest>